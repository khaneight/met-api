# All settings are optional; see src/config.ts for the full list.
PORT=3000
LOG_LEVEL=info
# Enables the persistent, shared Redis cache tier. Leave empty for in-memory only.
REDIS_URL=
# REDIS_URL=redis://localhost:6379
